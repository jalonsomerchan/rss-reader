---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ResetDataPage from '../../components/ResetDataPage.astro';
import { useTranslations } from '../../i18n/ui';

const locale = 'en';
const t = useTranslations(locale);
---

<BaseLayout title={t('reset.title')} description={t('reset.description')} locale={locale}>
  <ResetDataPage locale={locale} />
</BaseLayout>
